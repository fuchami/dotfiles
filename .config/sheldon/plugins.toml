# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

shell = "zsh"

[plugins]

# For example:
#
# [plugins.base16]
# github = "chriskempson/base16-shell"

[plugins.zsh-defer]
github = "romkatv/zsh-defer"

[templates]
defer = "{{ hooks?.pre | nl }}{% for file in files %}zsh-defer source \"{{ file }}\"\n{% endfor %}{{ hooks?.post | nl }}"
PATH = 'export PATH="$PATH:{{ dir }}"'

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"

[plugins.compinit]
inline = 'autoload -Uz compinit && zsh-defer compinit -d $ZSH_COMPDUMP'

[plugins.fzf]
inline = "source <(fzf --zsh)"

[plugins.zoxide]
inline = 'eval "$(zoxide init zsh)"'

[plugins.zeno]
github = "yuki-yano/zeno.zsh"
use = ["zeno-plugin.zsh"]

[plugins.oh-my-posh]
inline = 'eval "$(oh-my-posh init zsh --config $XDG_CONFIG_HOME/oh-my-posh/kanagawa.omp.toml)"'

[plugins.functions]
local = "~/.config/zsh/functions/"
use = ["*.zsh"]
apply = ['defer']

[plugins.gcloud-sdk]
inline = '''
brew_prefix="$(brew --prefix)"
if [ -f "$brew_prefix/share/google-cloud-sdk/path.zsh.inc" ]; then
  zsh-defer source "$brew_prefix/share/google-cloud-sdk/path.zsh.inc"
  zsh-defer source "$brew_prefix/share/google-cloud-sdk/completion.zsh.inc"
fi
'''

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"
apply = ["defer"]

[plugins.fast-syntax-highlighting]
github = "zdharma-continuum/fast-syntax-highlighting"
# apply = ["defer"]
